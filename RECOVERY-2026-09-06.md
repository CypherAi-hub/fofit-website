# FoFit website recovery — September 6, 2026

## Live restoration
The homepage returned HTTP 200 but rendered blank. Browser console showed `supabaseUrl is required`. The live JavaScript contained empty Supabase URL and public anonymous key constants. Production Vercel URL configuration was also empty and was restored from the existing local configuration without logging values.

An ordinary redeploy reused prebuilt output and did not fix the bundled constants. Recovered the exact 86 non-DS_Store files from deployment dpl_H2JNEWGybV3eTf4ChzXUigikXWE7 through Vercel. Changed only the compiled public Supabase configuration and removed its now-inaccurate missing-config warning. SHA-1 comparison against the deployment file inventory confirmed only assets/index-wXrNSfYp.js changed. Existing imagery, videos, page copy, pricing and routing were preserved.

Production repair: dpl_H8aCN3qJhLQjTgVGycF5TKGdv6qM, https://website-madngxvkf-cypherai-hubs-projects.vercel.app, aliased to https://www.fofit.app. Verified repaired bundle locally and verified live homepage visibly renders after browser reload. This was a direct authorized outage recovery deployment, not a git push.

## Local prevention changes
The local website checkout differs from the deployed website revision. Its source was not deployed over the live site. Added nullable Supabase initialization, safe unauthenticated provider state, unavailable-auth messages and waitlist guards so missing configuration cannot crash marketing pages. Added a narrowly matched repair script for the historical prebuilt bundle; it reads public configuration from environment variables and never prints values.

Validation: TypeScript passed after installing existing lockfile dependencies; Vite production build passed; copy and static legal-page checks passed. Explicit empty-URL and empty-key build rendered the homepage in a browser. Initial checks before npm ci could not run the real TypeScript/Vite tools; the subsequent installed-tool checks are the passing results.

## Limits and follow-up
Sign-in, account creation, waitlist submission, payments and full video playback were not tested. No pricing or Community copy changed. Existing live website prominently markets Community and paid tiers, while the current app brief calls for Community off-dock and a free launch; reconcile messaging before launch. The local source crash-prevention fix is committed locally only and is not included in the minimal live bundle repair. Future source deployments must use the correct production configuration and the actual source revision for the current design.

App Store media inventory and the ten-image recommendation are recorded in FoFit-Media-Selection.md in task outputs. No App Store media deleted or uploaded, preserving the earlier explicit no-upload instruction.
