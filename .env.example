VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_WAITLIST_ENDPOINT=

# Optional. Overrides the iOS beta TestFlight link. Leave blank to use the
# default baked into src/data/beta.ts. Not a secret — it ships to the client.
VITE_TESTFLIGHT_URL=
